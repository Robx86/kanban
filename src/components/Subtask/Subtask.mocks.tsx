import { SubtaskProps } from '@/components/Subtask/Subtask';

const base: SubtaskProps = {
  children: 'Research competitor pricing and business models',
  finished: true,
};


export const mockSubtaskProps = {
  base,
};
